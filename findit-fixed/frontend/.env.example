# Copy to .env.local for development overrides, or .env.production for CRA builds.
# After changing, restart `npm start` (CRA only reads env vars at startup).

# Backend URL the frontend should hit. Defaults to http://localhost:5000 if unset.
REACT_APP_API_URL=http://localhost:5000
